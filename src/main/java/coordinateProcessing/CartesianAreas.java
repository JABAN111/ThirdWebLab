package coordinateProcessing;

public interface CartesianAreas {
    boolean firstRotation(Integer x, Double y, Float R);
    boolean secondRotation(Integer x, Double y, Float R);
    boolean thirdRotation(Integer x, Double y, Float R);
    boolean fourthRotation(Integer x,Double y,Float R);
    boolean areaCheck(Integer x,Double y,Float R);
}
